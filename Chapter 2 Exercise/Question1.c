#include <stdio.h>

int main(){
    // Subquestion 1
    int a; int b = a; // It Works
    // Subquestion 2
    int v = 3 ^ 3; // It Works But Doesn't Give Value Of 3 To Power 3
    // Subquestion 3
    // char dt = "December"; // Not Works
    return 0;
}